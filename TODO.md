# Provider + Model Management TODO

- [ ] Add backend provider state storage in KV
- [ ] Add provider base URL normalization and endpoint builder
- [ ] Add `/api/nangua/*` admin APIs (session, upsert, delete, select, refresh-models)
- [ ] Wire `/api/session` and `/api/chat` to provider-aware config
- [ ] Add `/nangua` admin page (auth protected)
- [ ] Add provider/model selectors in chat request payload
- [ ] Verify lint and deploy readiness
